/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true,
        domains: ["media.licdn.com"], // add any hostnames you need here
      },
};

export default nextConfig;
